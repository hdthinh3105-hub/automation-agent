version: "3.9"

# Phase 1+2 scope: Postgres (with pgvector extension available) + Redis.
# API/Worker containers are commented out for now — in Phase 1 we run
# `npm run start:dev` locally against these two services. Dockerfiles for
# api/worker will be added when Phase 11 (Deployment) is implemented.

services:
  postgres:
    image: ankane/pgvector:v0.5.1
    container_name: acs-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_customer_support
    ports:
      - "5432:5432"
    volumes:
      - acs-postgres-data:/var/lib/postgresql/data
      - ./postgres/init-extensions.sql:/docker-entrypoint-initdb.d/init-extensions.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: acs-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - acs-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  acs-postgres-data:
  acs-redis-data:
